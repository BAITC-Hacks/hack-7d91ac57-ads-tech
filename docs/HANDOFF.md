# Журнал передачи между Claude Code и Codex

Формат: `HH:MM · кто · что сделал · что нужно от другого`. Дописывать в конец. Читать в начале каждой задачи.

- 13:00 · human · репозиторий инициализирован из шаблона · Claude: заполнить PLAN.md по ТЗ; Codex: ждать API.md v1
- 13:40 · claude · PLAN.md и API.md v2 готовы · Codex: строить дашборд по API.md (run → таблица по поставщикам, раскрытие строки с обоснованием, фильтры склад/категория/срочность, кнопки Утвердить/Экспорт, панель агента). Backend run будет в 14:00, до этого мокать по примерам JSON.
- 13:55 · claude · backend: pandas/openpyxl добавлены; пишу synth.py + replenish.py; run будет к 14:10
- 13:38 · claude · движок replenish.py считает (1.8 с на 41 позицию), эндпоинты пишу сейчас
- 13:41 · claude · backend: run готов. Все эндпоинты API.md работают (см. таблицу изменений 14:35). Запуск: make backend. Codex: переключай моки на реальные вызовы.
- 13:45 · claude · README v1 под кейс, verify.sh, Docker-образ с данными и тестами закоммичены. Жду фронт. Порт 8000 свободен: make backend.
- 13:50 · claude · backend запущен на :8000 (DEMO_MODE, автоперезагрузка), автокоммит каждые 20 мин запущен. Codex: можно бить в реальный API.
- 13:59 · claude · ДАННЫЕ ПАРТНЁРА подключены: backend перезапускается на DATA_DIR=data/partner. Для фронта: склад по умолчанию теперь берётся из /api/health → data.default_warehouse (у партнёра это «Алматы», у синтетики «Главный»); поставщиков 2 (IEK, Systeme Electric); артикулы вида 010300016_; категорий ~20; первый /api/replenish/run на партнёрских данных ~40 с, но при старте он предрассчитывается — в /api/health есть calculation_ready, показывайте «идёт расчёт» пока false. Поле orders[].article — артикул поставщика, показывать рядом с кодом.
- 14:00 · claude · Codex не начал фронт за 60 мин → Claude берёт frontend/ на себя с 14:00 (страховка). Codex: если начал позже, НЕ переписывай App.tsx, а бери задачи из HANDOFF.
