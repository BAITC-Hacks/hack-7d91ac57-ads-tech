# Журнал решений (заполнять по ходу, 1–2 строки на запись)

Честный отчёт о том, что пробовали, что отвергли и почему. Читается экспертами как свидетельство процесса; у победителей похожих хакатонов такой файл был.

| Время | Решение / попытка | Итог | Почему |
|---|---|---|---|
| 13:35 | Сценарий задан ТЗ: расчёт заказов поставщикам. Числа считает детерминированный движок, LLM только объясняет через инструменты | принято | объяснимость и устойчивость требуются ТЗ; результат не зависит от доступности модели; DEMO_MODE без ключа |
| 13:40 | Синтетические данные с «правдой» (сезонность, рост, дефициты, разовые заказы) вместо ожидания выгрузки партнёра | принято | ТЗ разрешает; известная правда позволяет писать тесты must-have |
| 14:10 | Выбросы: робастный z по медиане/MAD + доля одного клиента в месяце; помеченные транзакции заменяются типичным объёмом, а не удаляются | принято | удаление обнуляет день и занижает спрос; замена сохраняет «обычную» часть дня |
| 14:20 | Сезонность: ratio-to-trend с двумя итерациями и сжатием к 1 при короткой истории; тренд только при R² ≥ 0.2 | принято | Holt-Winters/Prophet отвергнуты: дольше внедрять, хуже объяснять менеджеру, 24 месяца истории мало для устойчивой оценки |
| 14:45 | Упущенный спрос: импутация + явный коэффициент за 12 мес к уровню | принято после теста | одна импутация почти не меняла прогноз из-за нормировки сезонных индексов; тест must-have 3 падал |
| 14:50 | Экспорт CSV `;` UTF-8 BOM и XLSX | принято | открывается в Excel и загружается в 1С без перекодировки |
| 14:10 | Партнёр выдал реальные выгрузки 1С (IEK, Systeme Electric). Пишем импортёр в формат движка вместо переделки движка под Excel | принято | движок и тесты не меняются; любые новые выгрузки подключаются через импортёр или CSV |
| 14:15 | Клиента в выгрузке нет → «один клиент» = одна накладная (Номер) | принято | одна накладная = один заказ одного покупателя; ровно то, что нужно правилу B |
| 14:20 | 2024 год дозаполняется из помесячной таблицы равномерно по дням | принято | транзакции есть только с 2025; так сезонность считается по 33 месяцам вместо 21 |
| 14:35 | Правило A выбросов ужесточено: + верхний 1 % транзакций артикула, порог 8× медианы | принято после прогона | на трубах/коробках старое правило отсекало по 90 накладных, это регулярный опт, а не разовые заказы |
| 14:40 | Файлы партнёра не коммитим, DATA_DIR переключает источник; по умолчанию синтетика | принято | коммерческие данные; эксперты запускают без них, а с файлами воспроизводят одной командой |
| 14:00 | Codex не приступил к фронту за час → Claude Code пишет дашборд сам | принято | без UI нет демо; контракт API.md уже был, поэтому фронт написан за 25 минут и собрался с первого раза |
| 14:05 | График на чистом SVG, без chart-библиотек | принято | нет зависимостей и времени на настройку; нужны только столбцы, линия, пунктир, заливка и маркеры |
| 14:10 | Дозаполненные строки 2024 (MONTHLY) исключены из детекции выбросов | принято после скриншота | равномерные месячные средние ловились правилом «один клиент = 100 % месяца» |
| 14:15 | Опциональный пункт ТЗ «тренды по категориям» как отдельный эндпоинт + свёрнутая таблица со спарклайнами | принято | дёшево (15 мин), закрывает пункт ТЗ, не мешает основному сценарию |
| 14:20 | Бизнес-слой: «заказать до», дата дефицита, сумма заказа, плановый прирост как параметр, отчёт об избытках и «мёртвом» товаре, письмо поставщику, импорт xlsx через UI | принято | закупщику нужен план действий и деньги, а не только список; избыток — прямая боль из ТЗ |
| 14:30 | Импорт: файл партнёра назван «Syseme Electric» (опечатка) → распознавание по подстроке «sys» | принято | реальные выгрузки содержат опечатки, импортёр должен быть терпимым |
| 14:35 | Параллельный расчёт через fork-пул, полный набор (include_zero) кэшируется и предрассчитывается при старте | принято | 40 с → 9–15 с на 3 192 артикула; отчёты об эффекте и избытках мгновенные |
| 14:35 | LLM-независимость вынесена в README и питч: локальная модель через Ollama/vLLM | принято | казахстанский бизнес может не покупать токены OpenAI; данные не покидают компанию |

## Отвергнутые подходы
- Прогноз через LLM ("спроси модель, сколько заказать"): невоспроизводимо, необъяснимо, нарушает требование устойчивости к аномалиям.
- Библиотеки прогнозирования (statsmodels ETS, Prophet): дополнительные тяжёлые зависимости, сложнее объяснить, нет времени на калибровку.
- Удаление выбросов целиком: занижает регулярный спрос в день выброса.

## Известные ограничения на 18:00
- 
